<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Blog-Waffle</title>
  </head>
  <body>
    <header>
      <nav>
        <ul>
          <li><a id="homeButton">Home</a></li>
          <li><a id="dashboardButton">Dashboard</a></li>
          {{#unless data.session.logged_in}}
          <li><a id="logInSignUpButton">Log In / Sign Up</a></li>
          {{/unless}}
          {{#if data.session.logged_in}}
          <li><a id="logOut">Logout</a></li>
          {{/if}}
          

        </ul>
      </nav>
    </header>

    <main>{{{body}}}</main>
  </body>
  <script src="/js/navbar.js"></script>
</html>